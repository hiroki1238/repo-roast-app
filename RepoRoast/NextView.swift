import SwiftUI

struct NextView: View {
    var body: some View {
        VStack {
            Text("新しいページ")
        }
    }
}

struct NextView_Previews: PreviewProvider {
    static var previews: some View {
        NextView()
    }
}
